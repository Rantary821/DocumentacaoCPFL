{
  "name": "CPFL WebApp",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt",
  "forwardPorts": [5000],
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  },
  "remoteUser": "vscode"
}